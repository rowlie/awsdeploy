version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install --no-cache-dir -r requirements.txt
run:
  runtime-version: 3.11.9
  command: python3 -m uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
